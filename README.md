# Strict_Job_Search_v3.2.01.21.26

Static GitHub Pages app.

- `rules.json` is the durable rules baseline.
- Sources are stored locally; first run preloads the authoritative slug stack.
- Mode buttons: Blue (state). Dirty cluster: Teal (transient).
